arr = [
  "+1 `Water` to all",
"+1 `Plant` to all",
"+1 `Animal` to all",
"temporary +1 to all skills to all",
"+1 AP to spend in current round",
"temporary +10% optimism",
"skip the next negative card",
"--1 `Water`",
"--1 `Plant`",
"--1 `Animal`",
"temporary -1 to all skills",
"-1 AP",
"can't participate next round",
"lose one of your stashed card",
]

import random

print(random.choice(arr))
